export type NavBar = {name: string, path: string}[];
export type Response = {code: number, };

export type Banners = {
    encodeId: string;
    exclusive: string; // 独家
    imageUrl: string;
    scm: string;
    targetId: number;
    titleColor: string;
    typeTitle: string;
}[];

